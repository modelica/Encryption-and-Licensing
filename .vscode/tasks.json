{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "generate keys for testing",
            "detail": "writes freshly generated keys to ./build/openssl_keys",
            "type": "shell",
            "command": "./scripts/generate_keys_for_testing.sh",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        }
    ]
}